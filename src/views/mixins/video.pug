mixin video(video)
  div
    h4
      a(href=`/videos/${video.id}`)=video.title
    ul
      li #{video.rating}/5
      li #{video.comments}개의 댓글
      li #{video.createdAt}에 작성됨.
      li #{video.views} 조회수