header
  a(href="/").header__logo
    i.fab.fa-youtube
  nav
    ul
      li
        a(href="/search")
          i.fas.fa-search
      if loggedIn
        li
          a(href="/videos/upload") 업로드
        li
          a(href="/users/edit") 프로필 수정
        li
          a(href="/users/logout") 로그아웃
        li
          a(href=`/users/${loggedInUser._id}`) 나의 프로필
            if !loggedInUser.avatarUrl
              span ☺️
            else
              if loggedInUser.avatarUrl.includes("https")
                img.header__avatar(src=loggedInUser.avatarUrl, alt="avatar")
              else
                img.header__avatar(src="/" + loggedInUser.avatarUrl, alt="avatar")
      else
        li
          a(href="/login") 로그인
        li
          a(href="/join").header__btn 가입